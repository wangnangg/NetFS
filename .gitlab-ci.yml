build:
  stage: build
  before_script:
    - rm -rf build
    - python3 genmake.py
  script: 
    - make all config=debug
    - make all config=release

test:
  stage: test
  script:
    - ./utest.sh "*" debug
    - ./utest.sh "*" release
